# Article assets

The animations in the published posts, and the scripts that generated them.

Each GIF is drawn by its `.py` neighbour with Pillow, so a diagram can be corrected by
editing the script and re-running it rather than by hand.

| File | Article section |
|---|---|
| `part1/agent-loop-vs-runner.gif` | Part 1, What runs the agent loop? |
| `part1/where-state-lands.gif` | Part 1, Where does state live? |
| `part1/revision-loop.gif` | Part 1, What if one agent isn't enough? |
