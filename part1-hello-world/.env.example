# ADK sections read this. Copy to .env inside the course_agent folder you are running.
GOOGLE_API_KEY="your-key-here"

# Strands defaults to Amazon Bedrock with Claude Sonnet 4, which uses your normal
# AWS credential chain. Set these or configure ~/.aws/credentials.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
